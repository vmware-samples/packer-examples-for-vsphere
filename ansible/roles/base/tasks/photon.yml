---
- name: "Updating the guest operating system."
  command: "{{ item }}"
  with_items:
    - "tdnf -y update"
- name: "Installing additional packages."
  command: |
    tdnf -y install \
    git \
    jq \
    logrotate \
    minimal \
    openssl-c_rehash \
    parted \
    tar \
    unzip \
    wget
